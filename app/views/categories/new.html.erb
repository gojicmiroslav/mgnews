<%= simple_form_for @category do |f| %>
	<%= f.input :name %>
	<%= f.input :menu_order, label: 'Menu Order', collection: 1..20 %>
  <%= f.submit 'Create Category', class: 'btn btn-primary' %>
<% end %>
